spring:
  application:
    name: config-server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
#        git: https://github.com/DkLicimer/config.git
#        searchPaths:

server:
  port: 8071


debug: true