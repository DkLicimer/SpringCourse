spring:
  application:
    name: config-server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 8071

debug: true
#
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/DkLicimer/config.git
